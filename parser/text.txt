var pom: integer;
function prvni(faktorial:boolean; splneno:boolean):boolean;forward;

function druha(pom:real; nesplneno_pom:boolean):boolean; forward;

function prvni(faktorial:boolean;splneno:boolean):boolean; 
var pom:integer;
begin

faktorial:=
end;
begin 
end.
var a:integer;
	vysl:integer;
	b:real;
	c:real;
	bo:boolean;
	s:string;
function factorial(n:integer; s:string;pomocna:real;b:boolean):integer;forward;
function factorial(n:integer; s:string;pomocna:real;b:boolean):integer;
var temp_result:integer;
	decremented_n:integer;
begin
	readln(b);
	if n<2 then
	begin	
		factorial:=1
	end
	else
	begin
		bo:=true;
		decremented_n:=n-1;
		temp_result:=factorial(decremented_n,'glo',5*2*3.0, true);
		factorial := n* temp_result
	end
end;

begin 
	s:='blop'; 
	write('Faktorial cislo pro vypocet faktorialu: ' );
	readln(a);
	c:=5/2*2*a+(a*5);
	if a<0 then
	begin
		write('factorial nelze spocitat'#10'' )
	end
	else
	begin
		vysl:=factorial(5+5,s,5*2/3,true);
		write('vysledek ', vysl, ''#10'')
	end	
end.